package codechef.CCDSAP.array

import scala.io.StdIn.{readInt, readLine}

// https://www.codechef.com/problems/COPS
object COPS {

  def main(args: Array[String]) {
    var T = readInt()
    while (T > 0) {
      val line1 = readLine().split(" ").map(_.toInt)
      val M = 56;
      val (m,x,y) = (line1(0),line1(1),line1(2))
      val copsAt = readLine().split(" ").map(_.toInt).sum


      T -= 1
    }
  }

}
